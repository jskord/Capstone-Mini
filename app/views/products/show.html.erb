  <div class="row">
    <div class="col-sm-6 col-md-4">
    <div class="thumbnail">  
    <img src= '<%= @product.image %>'/>
    <div class="caption">
    <h3><%= @product.name %></h3> 
    <p><%= @product.description %></p>
    <p text_color=red> <%= @product.sale_message %> </p>
    <h3>Price: $<%= @product.price %></h3>
    <p>Tax: $<%= @product.tax %></p>
    <p>Total: $<%= @product.total %> </p>
    <p><a href="#" class="btn btn-primary" role="button">Buy</a></p>
    </div>
    
    </div>
      
    </div>
  </div>
  <p>Back to all products<a href= '/products'> products</a></p>
  <div>
    <a href="/products/<%=@product.id%>/edit"> Edit this product </a>
    <div>
      <%= link_to "Destroy this product", "/products/#{@product.id}", method: :delete %>
    </div>
